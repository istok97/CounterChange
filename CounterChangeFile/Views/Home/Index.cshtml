@model IEnumerable<CounterChangeFile.Models.Repos>
@{
    ViewBag.Title = "Repository";
}

<table class="table">
    <tr><td>Name</td><td>Number</td></tr>
    @foreach (var item in Model)
    {
        <tr><td>@item.Name</td></tr>
    }
</table>